@{
    ViewData["Title"] = "Admin";
}

<div class="container mt-3">
    <div class="text-center">
        <h3>@ViewData["Title"]</h3>
    </div>
    <a class="btn btn-primary" asp-action="AddProject">@Localizer["AddProjectButton"]</a>
    <a class="btn btn-primary" asp-action="ModifProject">@Localizer["ModifProjectButton"]</a>
</div>